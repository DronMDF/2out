language: c

compiler:
  - gcc
  - clang

build:
  ci:
    - apt-get update
    - apt-get install cmake cppcheck doxygen ruby-dev uncrustify
    - gem install pdd
    - cmake .
    - make style
    - make VERBOSE=1
    - make test
