language: c

compiler:
  - gcc
  - clang

build:
  ci:
    - apt-get update
    - apt-get install cmake cppcheck ruby-dev
    - gem install pdd
    - ./style.sh
    - cmake .
    - make VERBOSE=1
    - make test
