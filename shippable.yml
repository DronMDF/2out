language: c

compiler:
  - gcc
  - clang

build:
  ci:
    - apt-get update
    - apt-get install cmake cppcheck ruby-dev
    - gem install pdd
    - cmake .
    - make style
    - make VERBOSE=1
    - make test
